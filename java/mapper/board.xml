<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board.BoardDAO">

    <select id="selectAll" resultType="com.playground.board.BoardVO">
        select board_no as boardNo, 
               title, 
               writer, 
               regdate, 
               viewcnt as viewCnt  
          from pg_board 
         order by board_no DESC
    </select>

    <select id="selectOne" resultType="com.playground.board.BoardVO">
        select board_no as boardNo, 
               title, 
               writer, 
               content, 
               regdate, 
               viewcnt as viewCnt 
          from pg_board 
         where board_no = #{boardNo}
    </select>

    <insert id="insertBoard" parameterType="com.playground.board.BoardVO">
        insert into pg_board (board_no, title, writer, content, regdate, viewcnt)
        values (seq_board_no.nextval, #{title}, #{writer}, #{content}, SYSDATE, 0)
    </insert>

    <update id="updateViewCnt">
        update pg_board 
           set viewcnt = viewcnt + 1 
         where board_no = #{boardNo}
    </update>
    
    <delete id="deleteBoard">
        delete from pg_board where board_no = #{boardNo}
    </delete>

</mapper>